{
    "name": "MongoDB Export",
    "url": "https://github.com/leoMehlig/mongodb-plugin-posthog",
    "description": "Sends events to a MongoDB database on ingestion.",
    "main": "index.js",
    "config": [
        {
            "key": "url",
            "hint": "URL of mongodb proxy server",
            "name": "Proxy URL",
            "type": "string",
            "required": true
          }
    ]
}
